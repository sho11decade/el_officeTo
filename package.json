{
  "name": "office-image-extractor",
  "version": "1.0.0",
  "description": "Extract and view images from Microsoft Office files",
  "main": "src/main.js",
  "homepage": "./",
  "scripts": {
    "start": "electron .",
    "dev": "NODE_ENV=development electron . --dev",
    "start:prod": "NODE_ENV=production electron .",
    "start:gc": "electron . --js-flags=\"--expose-gc\"",
    "build": "electron-builder",
    "build:win": "electron-builder --win --publish=never",
    "dist": "electron-builder --publish=never",
    "release": "electron-builder --publish=always",
    "postinstall": "electron-builder install-app-deps",
    "electron:clear-cache": "npx electron-builder install-app-deps --arch=x64",
    "rebuild": "electron-builder install-app-deps",
    "install:clean": "rm -rf node_modules package-lock.json && npm install",
    "audit": "npm audit",
    "audit:fix": "npm audit fix",
    "security:check": "npm audit --audit-level=high",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "electron",
    "office",
    "image",
    "extractor",
    "word",
    "excel",
    "powerpoint"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^37.2.4",
    "electron-builder": "^26.0.12"
  },
  "dependencies": {
    "mammoth": "^1.9.1",
    "sharp": "^0.34.3",
    "xlsx": "^0.18.5",
    "yauzl": "^3.2.0",
    "yazl": "^3.3.1"
  },
  "build": {
    "appId": "com.office-image-extractor.app",
    "productName": "Office Image Extractor",
    "directories": {
      "output": "dist",
      "buildResources": "assets"
    },
    "files": [
      "src/**/*",
      "assets/**/*",
      "!**/*.md",
      "!SECURITY_REPORT.md",
      "!.gitignore",
      "!node_modules/.cache"
    ],
    "compression": "normal",
    "nodeGypRebuild": false,
    "buildDependenciesFromSource": false,
    "npmRebuild": false,
    "electronDownload": {
      "cache": ".electron"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "assets/icon.png",
      "verifyUpdateCodeSignature": false,
      "requestedExecutionLevel": "asInvoker"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Office Image Extractor"
    }
  }
}
